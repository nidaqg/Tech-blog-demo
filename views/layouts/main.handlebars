<!DOCTYPE html>
<html lang="en=us">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Talk</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
  <link rel="stylesheet" href="../../public/css/style.css">
</head>

<body>

    <header>
    <img src="../../public/images/header.png" alt="header-image" class="img-fluid">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <section class="container-fluid justify-content-end">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
        <section class="collapse navbar-collapse justify-content-end" id="navbarLinks">
          <section class="navbar-nav"> 
            <a class="nav-link active" aria-current="page" href="/">Home</a>

            {{#if logged_in}}
            <a class="nav-link" aria-current="page" href="/dashboard">Dashboard</a>
            <a class="nav-link" aria-current="page" id="logout">logout</a>
             {{else}}
            <a class="nav-link" aria-current="page" href="/login">login</a>
            {{/if}}

          </section>
        </section>
      </section>
    </nav>
    </header>

    <main class="container container-fluid bg-light mt-4">
      <section class="row justify-content-center">
        <section class="col-sm-6 justify-content-center text-center">

      <!-- Render the sub layout -->
      {{{ body }}}
      </section>
      </section>
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>